package ao.kixima.supplierdev;

/** Espelha o enum Postgres/Prisma `SupplierDevTrack` (schema.prisma:1872-1876). */
public enum SupplierDevTrack {
    /** Credenciamento, licenças, certificações, conformidade. */
    BUROCRACIA,
    /** Procura de parceiro internacional / transferência de tecnologia. */
    PARCERIA,
    AMBOS,
}
