package ao.kixima.supplierdev;

/** Espelha o enum Postgres/Prisma `PlatformFeeStatus` (schema.prisma:370-373) — reutilizado aqui para a taxa de acesso. */
public enum PlatformFeeStatus {
    PENDENTE,
    COBRADO,
}
