# Não enviar para o contexto de build do Docker (mais rápido e evita conflitos).
**/node_modules
**/.git
**/dist
**/build
# Saída do Maven (backend-java): o jar compila-se dentro da imagem, nunca entra do disco local.
**/target
**/coverage
**/*.log
**/.env
**/.env.*
mockups
fin_mockups
